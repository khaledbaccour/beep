/** Hours before session start for full refund eligibility */
export const FULL_REFUND_HOURS = 24;

/** Hours before session start for partial refund eligibility */
export const PARTIAL_REFUND_HOURS = 2;

/** Percentage refunded for partial refunds (50%) */
export const PARTIAL_REFUND_PERCENTAGE = 50;

/** Minutes after scheduled start before marking as no-show */
export const NO_SHOW_THRESHOLD_MINUTES = 10;

/** Hours after session completion before funds are released to expert */
export const DISPUTE_WINDOW_HOURS = 24;

/** Grace period in minutes after session end time before auto-disconnect */
export const SESSION_GRACE_MINUTES = 5;
